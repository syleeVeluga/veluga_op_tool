# Veluga Op Tool — Frontend

React 19 + Vite + TypeScript SPA. GitHub Pages 배포.

## 실행

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # dist/ 생성
npm run lint
```

환경변수: `VITE_API_BASE_URL` (백엔드 URL), `VITE_BASE_PATH` (GitHub Pages base)
